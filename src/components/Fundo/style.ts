import styled from "styled-components"
import backgroundImg from '../../assets/background.png'

export const ContainerFundo = styled.div`
  /* background: red; */
  background: #ff0 url(${backgroundImg}) no-repeat ;
  background-size: cover;
  height: 100vh;
  padding-top: 20px;
`